var Promise = require('bluebird');

describe('Use Case Manage-Applications', () => {
  describe('Deploy-Workload Scenario', () => {
    it('Deploy-Workload Primary Test', (done) => {
       done();
    });
  });
});
