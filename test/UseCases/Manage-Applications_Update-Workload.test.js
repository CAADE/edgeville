var Promise = require('bluebird');

describe('Use Case Manage-Applications', () => {
  describe('Update-Workload Scenario', () => {
    it('Update-Workload Primary Test', (done) => {
       done();
    });
  });
});
