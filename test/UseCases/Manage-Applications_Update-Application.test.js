var Promise = require('bluebird');

describe('Use Case Manage-Applications', () => {
  describe('Update-Application Scenario', () => {
    it('Update-Application Primary Test', (done) => {
       done();
    });
  });
});
