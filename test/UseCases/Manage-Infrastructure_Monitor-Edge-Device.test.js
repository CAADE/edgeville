var Promise = require('bluebird');

describe('Use Case Manage-Infrastructure', () => {
  describe('Monitor-Edge-Device Scenario', () => {
    it('Monitor-Edge-Device Primary Test', (done) => {
       done();
    });
  });
});
