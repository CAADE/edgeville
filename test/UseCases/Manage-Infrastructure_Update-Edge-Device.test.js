var Promise = require('bluebird');

describe('Use Case Manage-Infrastructure', () => {
  describe('Update-Edge-Device Scenario', () => {
    it('Update-Edge-Device Primary Test', (done) => {
       done();
    });
  });
});
