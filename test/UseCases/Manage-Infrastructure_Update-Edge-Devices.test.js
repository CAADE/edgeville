var Promise = require('bluebird');

describe('Use Case Manage-Infrastructure', () => {
  describe('Update-Edge-Devices Scenario', () => {
    it('Update-Edge-Devices Primary Test', (done) => {
       done();
    });
  });
});
