var Promise = require('bluebird');

describe('Use Case Manage-Applications', () => {
  describe('Update-Model Scenario', () => {
    it('Update-Model Primary Test', (done) => {
       done();
    });
  });
});
