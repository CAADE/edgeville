var Promise = require('bluebird');

describe('Use Case Manage-Applications', () => {
  describe('Monitor-Workload Scenario', () => {
    it('Monitor-Workload Primary Test', (done) => {
       done();
    });
  });
});
