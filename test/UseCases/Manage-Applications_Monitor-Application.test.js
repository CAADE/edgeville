var Promise = require('bluebird');

describe('Use Case Manage-Applications', () => {
  describe('Monitor-Application Scenario', () => {
    it('Monitor-Application Primary Test', (done) => {
       done();
    });
  });
});
