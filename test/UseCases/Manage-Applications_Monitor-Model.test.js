var Promise = require('bluebird');

describe('Use Case Manage-Applications', () => {
  describe('Monitor-Model Scenario', () => {
    it('Monitor-Model Primary Test', (done) => {
       done();
    });
  });
});
