var Promise = require('bluebird');

describe('Use Case Manage-Applications', () => {
  describe('Update-Service Scenario', () => {
    it('Update-Service Primary Test', (done) => {
       done();
    });
  });
});
